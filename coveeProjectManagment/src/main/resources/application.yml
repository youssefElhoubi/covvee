spring:
  application:
    name: project-service
  datasource:
    url: jdbc:postgresql://localhost:3307/covvee
    password: 123
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

